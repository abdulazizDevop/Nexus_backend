# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
dist/
build/
*.whl

# Virtual environment
.venv/
venv/
env/

# Environment variables
.env
.env.*
!.env.example

# Claude Code lokal sozlamalari
.claude/

# Database
db.sqlite3
data/*.sqlite3
data/*.sqlite3-*
*.db

# Media uploads
media/

# Static collected
staticfiles/
static_collected/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Django
*.log
local_settings.py

# Coverage / Testing
htmlcov/
.coverage
.pytest_cache/
.tox/

# Celery
celerybeat-schedule
celerybeat.pid

# Misc
*.bak
*.tmp
*.temp

# Firebase service account (PRIVATE — must NEVER be committed)
firebase.json
firebase-credentials.json
firebase-*.json

# Google Cloud / Vertex ADC + service account key (PRIVATE — must NEVER be committed)
vertex-adc.json
application_default_credentials.json
*-sa-key.json
*service-account*.json
vertex_*.py

# APNS VoIP certificates (PRIVATE — must NEVER be committed)
*.p12
*.pem
*.key
*.b64
Certificates.p12
cert.b64
mediik_voip*.p12
voip_cert*.pem
voip_key*.pem
secrets/
panel/
web/

# Pharmacy raw Excel fayllar — admin paneldan upload qilinadi
dorilar/
/Makefile

# Infra/maxfiy hujjatlar (Bunny CDN token, DO Spaces key, pay rekvizitlari)
resourse/
